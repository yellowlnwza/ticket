# DATABASE CONFIGURATION (MySQL)
DB_HOST=127.0.0.1
DB_PORT=3308
DB_USER=root
DB_PASS=1234
DB_NAME=it_support

# ใช้สำหรับเข้ารหัส JWT token (ต้องไม่เปิดเผย)
JWT_SECRET=mySuperSecretKey12345
# อายุของ token (ตัวเลือก)
JWT_EXPIRE=8h

# FILE STORAGE PATH
UPLOAD_PATH=uploads/

# SERVER CONFIGURATION
PORT=4000
NODE_ENV=development

# OPTIONAL: EMAIL / NOTIFICATION (ถ้าเพิ่มในอนาคต)
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USER=your_email@gmail.com
MAIL_PASS=your_app_password